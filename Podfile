# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Chronocord' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Chronocord
  pod 'YALSideMenu', '~> 2.0'
  pod 'Charts'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'SwiftyJSON'

end
